# Database
DATABASE_URL="postgresql://fin:fin_dev_pw_change_me@localhost:5432/fin_dev?schema=public"

# Auth
JWT_SECRET="change-me-in-production"

# Storage (Dev: minio | Prod: s3 | Test: mock)
STORAGE_PROVIDER="minio"
MINIO_ENDPOINT="http://localhost:9000"
MINIO_ACCESS_KEY="fin_dev"
MINIO_SECRET_KEY="fin_dev_secret_change_me"
STORAGE_BUCKET="fin-docs-dev"
SIGNED_URL_TTL="60"

# Rate Limiting
DOWNLOAD_RATE_LIMIT_MAX="100"

# Note: Prod uses AWS S3 with SSE-KMS (AES-256 / 256-Bit at rest).
#       Dev simulates the same access pattern via MinIO (S3-compatible).
#       See: security/SECURITY_BASELINE_AWS.md in fin-tax-portal-specs
